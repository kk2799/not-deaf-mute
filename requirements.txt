# SPLASH — Python dependencies
# 深度学习底座由 Docker 基础镜像 demon-llm:v3 预装（torch/transformers/peft/accelerate 等）。
# 本文件记录项目实际依赖与已验证版本（容器内 2026-08-11），便于在新环境复现。
# 安装：pip install -r requirements.txt

# === 深度学习（镜像预装，列此仅作版本锚点）===
torch==2.4.1+cu118
torchaudio==2.4.1+cu118
transformers==5.7.0
peft==0.18.1
accelerate==1.5.0
datasets==4.0.0

# === 音频 ===
soundfile==0.14.0
librosa==0.11.0

# === 配置 / 实验 ===
hydra-core>=1.3
omegaconf==2.3.1

# === 数据 / 科学计算 ===
numpy==2.1.1
pandas==2.3.3
scikit-learn==1.9.0
pillow==10.2.0
openpyxl==3.1.5           # 读 ShipsEar_meta.xlsx

# === 追踪 / 可视化 ===
tensorboard==2.21.0
matplotlib==3.11.0
seaborn==0.13.2
tqdm==4.66.5
